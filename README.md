# IEEE Student Branch MIT-ADT — V2 Fixed

This ZIP is corrected so the project has a real `assets/` folder at the repository root.

Structure:

ieee-mit-adt/
├── assets/
│   └── avatar.svg
├── index.html
├── style.css
├── script.js
├── _headers
└── README.md

## Important
Upload the CONTENTS of this folder to GitHub, not the ZIP and not the parent folder.

The avatar is a local vector approximation of the avatar visible on the public reference page. If the branch has the original approved logo file, replace `assets/avatar.svg` with that official asset and update the two references in `index.html` if needed.

The site contains no analytics, trackers, backend, login, API keys, forms, or database.
