const esc=s=>String(s??'').replace(/[&<>"']/g,c=>({'&':'&amp;','<':'&lt;','>':'&gt;','"':'&quot;',"'":'&#39;'}[c]));
async function load(){try{const r=await fetch('/api/public',{headers:{Accept:'application/json'}});if(!r.ok)throw 0;const d=await r.json();if(d.settings.handle)handle.textContent=d.settings.handle;if(d.settings.subtitle)subtitle.textContent=d.settings.subtitle;if(d.settings.avatar_url)avatar.src=d.settings.avatar_url;links.innerHTML=(d.links||[]).map(x=>`<a class="link-card" href="${esc(x.url)}" target="_blank" rel="noopener noreferrer"><span class="icon">${x.kind==='instagram'?'<svg viewBox="0 0 24 24" aria-hidden="true"><rect x="3" y="3" width="18" height="18" rx="5"/><circle cx="12" cy="12" r="4.2"/><circle cx="17.5" cy="6.5" r="1" fill="currentColor" stroke="none"/></svg>':x.kind==='ieee'?'IEEE':'↗'}</span><span>${esc(x.title)}</span><span class="dots">•••</span></a>`).join('');announcements.innerHTML=(d.announcements||[]).map(x=>`<article class="notice"><b>${esc(x.title)}</b><p>${esc(x.body)}</p></article>`).join('');events.innerHTML=(d.events||[]).map(x=>`<article class="event">${x.image_url?`<img src="${esc(x.image_url)}" alt="">`:''}<h2>${esc(x.title)}</h2><p>${esc(x.date_text)}</p><p>${esc(x.description)}</p>${x.registration_url?`<a href="${esc(x.registration_url)}" target="_blank" rel="noopener noreferrer">Register →</a>`:''}</article>`).join('')}catch(e){}}
load();
